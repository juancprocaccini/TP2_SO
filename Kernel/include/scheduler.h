#ifndef SCHEDULER_H
#define SCHEDULER_H

#include <stdint.h>

/*
 * Interfaz del scheduler (Round Robin con prioridades).
 * Implementaciones en Kernel/processes/.
 */

// void   scheduler_init(void);
// void   scheduler_add(uint64_t pid);
// void   scheduler_remove(uint64_t pid);
// uint64_t scheduler_next(void);
// void   scheduler_tick(void);   // llamado desde el IRQ 0 (timer)
// void   scheduler_block(uint64_t pid);
// void   scheduler_unblock(uint64_t pid);

#endif
